#include<bits/stdc++.h>
using namespace std;
// sign assign kro + ya -
// same as S1-S2=D
int main()
{
    vector<int> arr={1,2,3,1};
    int n=arr.size();
    int target=3;
    // same as partition sum difference
    
}